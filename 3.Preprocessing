#We can use preprocessMethod() can tell us what method is applied for both data (it is useful for GRChannelSet, too)
preprocessMethod(mset)
#There is no preprocess method suitable for preprocessed data.
#Only preprocessQuantile() can be applied to signal intensities data.
mset=preprocessQuantile(mset)
